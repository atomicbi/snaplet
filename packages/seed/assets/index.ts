const createSeedClient = () => {
  throw new Error(
    "@atomicbi/snaplet-seed client is missing. Please use npx @atomicbi/snaplet-seed sync or npx @atomicbi/snaplet-seed init to generate the client.",
  );
};
export { createSeedClient };
